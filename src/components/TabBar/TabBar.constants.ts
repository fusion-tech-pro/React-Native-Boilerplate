import { wp } from 'src/utils/responsive';

export const TAB_BUTTON_SIZE = wp(40);

export const TAB_INDICATOR_SIZE = wp(32);

export const TAB_BAR_HORIZONTAL_PADDING = wp(24);

export const TAB_BAR_HORIZONTAL_MARGIN = wp(16);

export const TAB_INDICATOR_ANIMATION_DURATION = 300;
